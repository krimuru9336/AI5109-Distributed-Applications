# Jonas Wagner - 1315578

# Database properties
spring.datasource.url=jdbc:mysql://localhost:3306/WhatsDownDB
spring.datasource.username=root
# TODO Remove password from here
spring.datasource.password=<password>
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Additional properties (optional)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# Set maximum allowed file size to 10 megabytes
spring.servlet.multipart.max-file-size=10MB

# Set maximum allowed request size to 20 megabytes (includes file and other form data)
spring.servlet.multipart.max-request-size=20MB

azure.storage.account-name=whatsdown
# TODO Remove key from here
azure.storage.sas-key=<sas-key>
# TODO Remove connection string from here
azure.storage.connection-string=<connection-string>